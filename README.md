# بيننا

الملفات:
- `index.html`
- `styles.css`
- `app.js`
- `firebase-config.js`

قبل التشغيل:
1. افتح `firebase-config.js`.
2. ضع بيانات Firebase الحقيقية بدل القيم المؤقتة.
3. غيّر `channelId` فقط إذا أردت محادثة مختلفة.

التشغيل:
- افتح `index.html` محليًا للتجربة.
- أو ارفع الملفات على GitHub Pages.
